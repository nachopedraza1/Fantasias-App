
import { Grid } from "@mui/material"
import { NavBar } from '../../ui/NavBar';

export const Header: React.FC = () => {
    return (
        <Grid container >
            <NavBar />
        </Grid>
    )
}
